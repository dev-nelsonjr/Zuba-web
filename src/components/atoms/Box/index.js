import styled from 'styled-components'

import {
  background,
  margin,
  padding,
  flexbox,
  typography,
  color,
} from 'styled-system'

export const Box = styled('div')`
  ${typography}
  ${color}
  ${background}
  ${padding}
  ${margin}
  ${flexbox}
`
